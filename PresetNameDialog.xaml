<Window x:Class="PilotEars.PresetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save preset"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#1f2229" Foreground="#ebebf0"
        FontFamily="Segoe UI"
        WindowStyle="ToolWindow">
    <StackPanel Margin="22" MinWidth="320">
        <TextBlock x:Name="PromptText" Text="Preset name:" FontSize="13" Margin="0,0,0,8"/>
        <TextBox x:Name="NameBox" Padding="8,6" FontSize="14"
                 Background="#2a2d35" Foreground="#ebebf0" BorderBrush="#3a3d45"
                 KeyDown="NameBox_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="CancelBtn" Content="Cancel" Click="Cancel_Click"
                    MinWidth="90" Padding="12,7" Margin="0,0,8,0"
                    Background="#2a2d35" Foreground="#ebebf0" BorderThickness="1" BorderBrush="#3a3d45"
                    FontWeight="SemiBold" Cursor="Hand"/>
            <Button x:Name="OkBtn" Content="Save" Click="Ok_Click" IsDefault="True"
                    MinWidth="90" Padding="12,7"
                    Background="#4a9eff" Foreground="White" BorderThickness="0"
                    FontWeight="SemiBold" Cursor="Hand"/>
        </StackPanel>
    </StackPanel>
</Window>
